<a href="/dashboard"><button type="button" class="btn btn-outline-danger btn-sm">Back</button></a>
<hr>
{{!-- main container for page --}}
<div class="container">
    <form>
        {{!-- title of post --}}
        <h2>{{postText.title}}</h2>
        {{!-- change title --}}
        <div class="form-group">
            <h4>Title</h4>
            <input type="text" class="form-control" id="changeTitle" value="{{postText.title}}">
        </div>
        {{!-- change content --}}
        <div class="form-group">
            <h4>Description</h4>
            <textarea class="form-control" id="changeDescription" rows="3">{{postText.description}}</textarea>
        </div>
        <button type="submit" class="btn btn-danger pageBtn" id="uPost">Update Post</button>
        <button type="submit" class="btn btn-danger pageBtn" id="dPost">Delete Post</button>
        <br>
    </form>
</div>

{{!-- javascript connection --}}
<script src="/js/update.js"></script>